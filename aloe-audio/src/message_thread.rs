crate::ix!();

pub fn update_on_message_thread(updater: &mut AsyncUpdater)  {
    
    todo!();
        /*
            if (MessageManager::getInstance()->isThisTheMessageThread())
            updater.handleAsyncUpdate();
        else
            updater.triggerAsyncUpdate();
        */
}
