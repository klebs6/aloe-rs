crate::ix!();

/**
  | helper class for automatic conversions
  |
  */
pub struct CAPtrRef<T> {
    ptr: *mut T,
}

impl<T> CAPtrRef<T> {

    pub fn new(ptr: *mut T) -> Self {
    
        todo!();
        /*
        : ptr(ptr),

        
        */
    }
}
