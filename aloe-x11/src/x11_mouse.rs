crate::ix!();

pub fn get_logical_mouse_pos<EventType>(
    e:            &EventType,
    scale_factor: f64

) -> Point<f32> {

    todo!();
        /*
            return Point<float> ((float) e.x, (float) e.y) / scaleFactor;
        */
}

