crate::ix!();

pub fn ncos<FloatType>(
    order: usize,
    i:     usize,
    size:  usize

) -> FloatType {

    todo!();
    /*
        return std::cos (static_cast<FloatType> (order * i)
                          * MathConstants<FloatType>::pi / static_cast<FloatType> (size - 1));
    */
}

