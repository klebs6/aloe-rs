crate::ix!();

pub fn table_header_menu_callback(
        result:            i32,
        table_header:      *mut TableHeaderComponent,
        column_id_clicked: i32)  {
    
    todo!();
        /*
            if (tableHeader != nullptr && result != 0)
            tableHeader->reactToMenuItem (result, columnIdClicked);
        */
}

