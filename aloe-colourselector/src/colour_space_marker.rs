crate::ix!();

pub struct ColourSpaceMarker<'a> {
    base: Component<'a>,
}

impl<'a> Default for ColourSpaceMarker<'a> {
    
    fn default() -> Self {
        todo!();
        /*


            setInterceptsMouseClicks (false, false);
        */
    }
}

impl<'a> ColourSpaceMarker<'a> {
    
    pub fn paint(&mut self, g: &mut Graphics)  {
        
        todo!();
        /*
            g.setColour (Colour::greyLevel (0.1f));
                g.drawEllipse (1.0f, 1.0f, (float) getWidth() - 2.0f, (float) getHeight() - 2.0f, 1.0f);
                g.setColour (Colour::greyLevel (0.9f));
                g.drawEllipse (2.0f, 2.0f, (float) getWidth() - 4.0f, (float) getHeight() - 4.0f, 1.0f);
        */
    }
}

