crate::ix!();

pub fn code_editor_menu_callback(
    menu_result: i32,
    editor:      *mut CodeEditorComponent

) {
    
    todo!();
        /*
            if (editor != nullptr && menuResult != 0)
            editor->performPopupMenuAction (menuResult);
        */
}

