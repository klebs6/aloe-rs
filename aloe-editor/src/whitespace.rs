crate::ix!();

pub fn find_first_non_whitespace_char(line: &str) -> i32 {
    
    todo!();
        /*
            auto t = line.text;
            int i = 0;

            while (! t.isEmpty())
            {
                if (! t.isWhitespace())
                    return i;

                ++t;
                ++i;
            }

            return 0;
        */
}
